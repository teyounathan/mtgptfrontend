import { Component } from '@angular/core';

@Component({
  selector: 'app-layout',
  templateUrl: './app.layout.component.html',
})
export class AppLayoutComponent {
  // @Output() sidebarToggle = new EventEmitter<boolean>();
  // @Output() verticalMenuToggle = new EventEmitter<boolean>();
  // Username="username"
  // isShrunk = false;
  // isVerticalMenuShrunk = false;

  // toggleVerticalMenu() {
  //   this.isVerticalMenuShrunk = !this.isVerticalMenuShrunk;
  //   this.verticalMenuToggle.emit(this.isVerticalMenuShrunk);
  // }

  // toggleSidebar() {
  //   this.isShrunk = !this.isShrunk;
  //   this.sidebarToggle.emit(this.isShrunk);
  // }

  // closeVerticalNav(){
    
  // }
}
